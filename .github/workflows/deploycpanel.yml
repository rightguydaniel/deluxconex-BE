# name: Deploy Node.js Backend

# on:
#   push:
#     branches: [master]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
      
#       - name: Build Project
#         run: |
#           npm install
#           npm run build
          
#       - name: Deploy via SSH
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.SSH_HOST }}
#           username: ${{ secrets.SSH_USERNAME }}
#           port: 21098
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           script: |
#             rm -rf /home/elitfigj/api.deluxconex.com/*
#             cp -a dist/. /home/elitfigj/api.deluxconex.com/
#             chown -R elitfigj:elitfigj /home/elitfigj/api.deluxconex.com